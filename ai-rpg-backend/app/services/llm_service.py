from app.core.config import supabase

async def retrieve_memories(api_key, action_text, client):
    # This is a stub for now, as it was a stub in main.py
    return ''

async def store_memory(api_key, fakt, client):
    # Stub
    pass
